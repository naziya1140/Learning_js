// when you go through a web browser, learning js, then print window, and window.document.
//         document ka object on which model? 
//         doctype->document->html->head->body.
//         This is document Object model.
//         window->document->html->
//         child1->head->    title->text node(text value), meta->attribute, 
//         child2->body->    div->attribute, h1->text node, attribute, p->text node 

//         this basically helps in reaching particular place, know about their props,sibling, id, class etc.

//         console.log(document.links)//to get all the links in html collection not in array, but they can be converted to array.
//         console.log(document.links[0]);
//         document.getElementbyId('xyz');//you can do manipulation on this.

//         document.getElementbyId('xyz').innerHTML= "<h1>Chai aur code</h1>"//dom manipulation.
